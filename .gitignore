node_modules/
package.json
package-lock.json
*.xlsm
_xlsm_extract/
.claude/*
!.claude/hooks/
!.claude/skills-vendor/
!.claude/settings.json
.claude/settings.local.json

# Local QA test credentials (never commit)
tools/.qa-credentials

# Personal Azure learning-environment config — never committed. See
# azure/RUNBOOK.md. Keeps Azure test credentials out of the file GitHub
# Pages serves live from config.js.
config.local.js

# Azure deployment transcript — contains the database password. Never commit.
azure/.deploy-output.txt

# Local-auth JWT signing secret — must match PGRST_JWT_SECRET. Never commit.
azure/.local-auth-secret
